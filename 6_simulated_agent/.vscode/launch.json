{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: agent.py",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/tests/agent.py",
            "console": "integratedTerminal",
            "justMyCode": true,
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}
